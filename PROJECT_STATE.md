# FreeISP CAD - current source of truth

## STATUS 19 Aug 2026 - READY TO PRINT (coupons first)

All 141 offline checks pass on the workspace AND the deployed Fusion tree,
and both trees are byte-identical.  Everything below "Current user decisions"
is the running log; this block is what is true right now.

### The parts to print, in order

| # | Fusion script | Part | Size (mm) | Print orientation |
|---|---|---|---|---|
| 1 | `FIR_FitCoupons` | 3 coupon PAIRS (6 bodies) | small | as built, flat |
| 2 | `FIR_Shell` | the tub | 282.4 x 278.2 x 80 | floor down (flat on bed) |
| 3 | `FIR_BottomLid` | port face | 280 x 83 + shelf | as built |
| 4 | `FIR_CurvedLid` | curved cover | 280 x 114 x 66 | STANDING ON AN END WALL |
| 5 | `FIR_TopLid` | deep cap | 291 x 286 x 55 | roof down - ONLY 5mm bed margin: centre it, no brim |
| 6 | `FIR_ModuleGadget` | brain case | settled earlier | per its script |
| 7 | `FIR_ModulePlate` | electronics tray | settled earlier | per its script |

ONE SCRIPT PER PART (owner, 19 Aug): FIR_Shell builds only the tub; the deep
cap moved to the new `FIR_TopLid`; the horn sled NO LONGER EXISTS - the horn
bolts directly to two floor pads (M4 x 10, rear foot holes, apex unused); and
the tamper reed/magnet senses the CURVED LID only (the top-cap pair was
removed - the cover must come off first anyway, so that is the alarm event).

DO NOT print the cap (#5) until the Landzo 3.5" screen is measured - its
window and seat are typical-shield ASSUMPTIONS (`SCREEN_MEASURED = False`)
and the popup nags about it.  Coupons (#1) gate everything: the pairs must
slide, click, screw and reopen per the pass criteria in their popup.

### Bought hardware this design expects

- 2 wall screws 4.5 x 40 + plugs (the box hangs on their heads)
- M3 self-tappers: 4 cap sides, 6 port face, 2 cover locks (+ the brain
  stack's own M3s, settled earlier)
- M4 x 10: 2 horn wing screws; M4 x 8: 3 horn foot bolts (bench, into sled)
- 2 magnets D12.1 x 4.7 (measured), 2 reed switches, green + red 5mm LEDs
- Landzo 3.5" TFT (UNO/Mega shield) for the cap roof window

### Open items (all flagged in the Fusion popups too)

1. MEASURE the Landzo screen (PCB outline, glass, visible area) -> then the
   cap. 2. BENCH-TEST reed+magnet at ~10.5mm in both orientations (head-on
   for the cover pair, face-down/beside for the cap pair). 3. Heat-set
   inserts: geometry is one flag per closure; needs the purchase + the real
   insert's bore. 4. Still unmeasured: horn foot 99mm (sled only), switch
   jack height 11.5mm, adapter 46x46x52. 5. Wall-plate load test is OBSOLETE
   (no plate any more); the keyhole mount hangs on two screws - sanity-check
   on the wall with the empty tub before loading it.

### Final design decisions (vs the dead ends they replaced)

- MOUNT: two BLIND keyholes in the floor, hang on two wall screws; watertight
  (2mm inner skin never cut).  Replaced: back-wall keyholes, cleat plate,
  back-wall ears, projecting tabs (unprintable at 328mm), through-floor
  screws, two-rail hanger.
- ORIENTATION: wall panel - floor against the wall, +Z out of the wall, port
  face runs DOWN.  Internal retention designed for this (owner) - SETTLED.
- CAP: 4 screws (2 per side) in visible counterbored pads + 6 snap detents +
  lead-in chamfer; INDOOR variant carries the centred screen window + 2 LED
  holes (INDOOR_SCREEN=False restores the sealed weatherproof roof).
- COVER SELF-CLICK (owner, 31 Aug: the printed cover "just falls off - the
  rails are only a guide"): stepped detent bumps on each BottomLid rail's
  outboard face (0.8 catch / 0.4 lead-in, lid z14-20) snap into an 8mm gap in
  the CurvedLid's outboard channel ribs (cover z47-55). Only the short rib
  tail (z55-63) ever rides the bump, over the last ~14mm of travel, so the
  slide stays free until it clicks. Interference 0.5mm; a firm pull frees it;
  the 2 lock screws stay the security lock. Coupon pair 3 carries the detent
  - reprint that pair to feel the click before reprinting the whole lid.
  (The already-printed bottomlid.stl predates the bumps.)
- FRONT: BottomLid with seated screws + reed groove; CurvedLid CURVED to the
  roof, landing FLUSH at Z120 with one 0.3mm shadow line (Ubiquiti-style
  seamless joint) - cover therefore slides on LAST, after the cap.
- TAMPER: two reed+magnet pairs (cover ~10.3mm, cap ~10.4mm), press-fit
  D12.05 pockets, nothing visible outside.
- HORN: bench-bolted to its sled, two wing screws, NO cage (owner).
- PRINTER: Ender 3 Plus 300x300x340 is a hard envelope check in the harness.
- VERIFICATION: `python tools/fusion_offline_check.py [deployed_dir]` after
  every change - it measures real Fusion behaviour (plane conventions were
  probe-measured after the floating-screws saga) and checks both trees.


## Active CAD task

The LARGE SHELL is print-ready (see STATUS above); the small brain case was
settled earlier.  Current activity: print the fit coupons, measure the
screen, then print the shells in the order listed.

## Edit and run this exact Fusion script

- Workspace master: `fusion/FIR_ModuleGadget/FIR_ModuleGadget.py`
- Fusion deployed copy: `%APPDATA%/Autodesk/Autodesk Fusion 360/API/Scripts/FIR_ModuleGadget/FIR_ModuleGadget.py`

Fusion currently runs the deployed copy, so keep it synchronized with the workspace master after every edit. Run the script in a **fresh Fusion design**; it deletes its old `FIR Module`, `FIR Brain Case`, and `CHECK:` bodies before rebuilding.

## Fusion launcher - scripts the user runs

The user runs project CAD through **Utilities > Scripts and Add-Ins** in Fusion. Every project script is loaded from `%APPDATA%/Autodesk/Autodesk Fusion 360/API/Scripts/<script>/<script>.py`; its editable workspace master is `fusion/<script>/<script>.py`.

Installed project scripts recorded from the launcher:

- `FIR_Assembly`
- `FIR_BottomBox`
- `FIR_BottomLid`
- `FIR_BoxBottom`
- `FIR_BoxFront`
- `FIR_CurvedLid`
- `FIR_Enclosure`
- `FIR_Faceplate`
- `FIR_Frame`
- `FIR_FullBox`
- `FIR_GadgetPlateCheck`
- `FIR_MainBox`
- `FIR_Mikrotik`
- `FIR_Mini`
- `FIR_Module`
- `FIR_ModuleBase`
- `FIR_ModuleGadget`
- `FIR_ModuleOpen`
- `FIR_ModulePlate`
- `FIR_OverhangTest`
- `FIR_PortPlate`
- `FIR_Shell`
- `FIR_ShellCheck`
- `FIR_ShellTub`
- `FIR_TestTile`
- `FIR_Tray`

`CustomGraphicsSample` and `ExtractBOM` also appear in the Fusion launcher, but they are Autodesk/sample utilities and are **not** FreeISP CAD sources.

### Required edit workflow

1. Identify the exact `FIR_*` script the user selected in Fusion.
2. Edit its workspace master under `fusion/<script>/`.
3. Synchronize the changed `.py` and `.manifest` into the matching `%APPDATA%` Fusion script folder.
4. Ask the user to rerun that same named script in a fresh Fusion design. Do not claim a visual change until the deployed copy is synchronized and run.

### Shared mechanical interface (do not duplicate these values)

- Canonical source: `fusion/_shared/FIR_Interface.py`.
- Deployed copy: `%APPDATA%/Autodesk/Autodesk Fusion 360/API/Scripts/_shared/FIR_Interface.py`.
- `FIR_ModulePlate`, `FIR_ModuleGadget`, `FIR_Shell`, and `FIR_ShellCheck` load this one contract for tray dimensions, PCB dimensions/hole pattern, the +X/+Y cap offsets, the four case roof holes, the derived cap-boss pattern, the brain case's outer envelope (`CASE_OUTER_W/H`, `CASE_BODY_Z`), the Tenda switch's envelope/position/port slot/cable notches (`POE_CX`, `poe_cable_notch_x()`, `poe_jack_air()`), and the whole alarm-horn installation (`horn_body()`, `horn_mount_points()`, `HORN_VENTS`). Edit the shared file first; do not hand-edit a duplicate coordinate list in an individual part. `FIR_Interface.validate()` raises on import if the contract is self-inconsistent, so a bad edit fails loudly in Fusion instead of quietly building wrong geometry.
- The deployed `_shared` folder must be synchronized whenever the common source or any consuming script changes. `FIR_ModulePlate` retains its deployed workspace-loader wrapper; do not replace that wrapper with a stale copy.

## Current user decisions

- The brain PCB mounts inside the **small brain case**, not directly to `FIR_Shell` / the large top casing.
- The small brain case attaches directly to the current `FIR_Shell` deep top cap using four reinforced internal bosses at cap X = **-11.5 / +105.5** and Y = -30/+50. Each cap boss is 9 mm OD x **10.8 mm** high with a 13 mm roof-root flange and a 2.6 mm pilot. The case is installed 10 mm toward cap +Y and **47 mm toward cap +X** (the shift that makes room for the alarm horn on the switch side); its four symmetric local roof holes are `(-58.5,-40)`, `(58.5,-40)`, `(-58.5,40)`, `(58.5,40)`. This moves the formerly corner-adjacent pair 20 mm inward for easy screwdriver access. Use four low-profile M3 x 12 self-tapping screws from inside the empty case upward into the cap pilots: 3 mm through the case roof plus 9 mm thread engagement, leaving 1.8 mm at the end of the cap boss. The shorter hidden boss lifts the new 69.6 mm case to 1.1 mm over the MikroTik; no external ears are used.
- The deep top cap retains a continuous 3 mm roof.  Its brain-case boss flanges begin exactly at the roof's inside face, so they are fused into the roof rather than floating below it.  `FIR_ShellCheck` must mirror this same roof/boss stack.
- **Assembly order is mandatory:** (1) tighten the four case-to-cap screws upward from inside the empty brain case; (2) install and screw down the brain PCB; (3) install and secure the populated electronics tray from the open bottom; (4) assemble the cap/case onto the large shell.  Do not try to tighten the cap screws after the PCB is installed.
- **PCB orientation / vertical contract:** never edit the PCB itself.  Its populated silkscreen/component face points **DOWN** toward the ModulePlate; its bare solder-tail face points UP toward the small-case roof.  The measured face-to-face distance from the ModulePlate flat top at local Z = 3.0 mm to the PCB component face is **52.0 mm** (allowed inspection range 50.0–55.0 mm).  In `FIR_ModuleGadget`, the component face is Z = 55.0 mm and the solder face is Z = 56.6 mm.  `FIR_GadgetPlateCheck` must display and fail this contract if it goes outside the allowed range.
- **Three fastening systems — never interchange them:** (1) tray: four M3 self-tappers come from below through `FIR_ModulePlate` into the mid-edge, wall-embedded tray bosses; (2) PCB: four **M3 x 10** board screws enter upward from the open component-side bottom through the unchanged PCB holes and take 8.4 mm of grip in the short roof-post pilots; (3) case-to-cap: four low-profile M3 x 12 screws start inside the empty brain case, pass upward through roof clearance holes, and thread into the large top-cap bosses.
- The four PCB roof posts are internal-only reinforced structures above the bare solder side: 9 mm core, 13 mm roof collar, a 2.6 mm blind pilot matched to M3 x 10, and two 3 mm ribs per post.  The post zone is 10 mm high; collar and ribs overlap 0.5 mm into the roof for a true structural Fusion/print join, while preserving a 2.5 mm outer roof skin.  The ESP32/socket and other PCB component envelopes hang **DOWN** into the 52 mm plate-side gap.  These changes must never create exterior roof protrusions or alter the flat outer roof.
- The small case must retain a completely smooth outer silhouette: its four roof-mount holes use internal-only 9.5 mm load-spreading pads, embedded 0.5 mm into the roof for a true Fusion join. Their 6.4 mm internal counterbores and 3.4 mm M3 clearances start below the pad face, so all four holes are visibly open from the cavity to the outside. Never add rectangular ribs or other reinforcement that crosses the rounded exterior outline or creates corner tabs/protrusions.
- The ModulePlate is a **snug hand-push fit** in the 129 x 129 mm R9.5 Gadget pocket: tray outer dimensions are 128.3 x 128.3 mm with R9.15 corners, leaving a controlled 0.35 mm on each side/arc. This replaces the physically loose old 125 mm tray. Its 0.5 mm underside entry chamfer avoids elephant-foot jamming. The tray is seated on one continuous rounded internal ledge ring: outer 130 x 130 mm R10, inner 126.3 x 126.3 mm R8.15, maintaining 1 mm support under the plate and no exterior corner tabs.
- **Tray seating datum:** `FIR_ModulePlate` is local Z = 0..3 mm and its top face contacts the underside of the ModuleGadget ledges beginning at local Z = 3 mm. Do not translate the assembled plate upward by `PLATE_SEAT_Z`; that would overlap the ledges and tray-boss bases. `FIR_GadgetPlateCheck` is the dedicated actual-case + actual-plate inspection script.
- The four mid-edge `TRAY_MOUNT` posts are only for fixing the 128.3 x 128.3 electronics tray. They are reinforced 9 mm bosses with 11 mm feet, a clearly visible 4.6 mm entry mouth leading to a 2.6 mm threaded pilot, and 12 mm wall-embedded ribs; they are not top-shell mounts.
- The PCB J4 (TFT) and J5 (RC522) edge faces **+X**.
- The +X case wall has one uninterrupted **50 x 5 mm** J4/J5 Dupont-wire slot, centred at local Z = 29 mm (Z = 26.5..31.5): exactly halfway between the ModulePlate top at Z = 3 and the downward PCB component face at Z = 55. This gives the wires a straight drop before they turn outward. It has no decorative border, recess, `POWER` / `INTERNET` labels, divider, or LED holes.
- The dedicated BottomLid 10 mm power-cable pass-through and CurvedLid power notch share the X = +10 mm centreline.  This is a cable-route correction only; measured MikroTik, PoE, RJ45, jack, switch, and other port openings must not be changed unless the user explicitly requests it.
- **Confirmed PoE switch / long-port decision:** the actual switch is 82 mm face width x 52 mm depth x 23 mm height, retained at shell X = -85 with its front against the BottomLid.  Its five guessed individual openings are replaced only in `FIR_BottomLid` by one 68.8 x 11.5 mm rectangular service slot (the requested approximately 69 x 11.5): local centre X = +85, Y = 15.45; assembled centre X = -85, Z = 15.45.  The 68.8 mm value preserves the supplied 6.6 mm side lands exactly.  The slot lower edge is 3.2 mm above the switch base.  This makes the remaining top land 8.3 mm; do not silently change it to 7 mm without a new owner decision.  `FIR_Shell` and `FIR_ShellCheck` must keep the same 82 x 52 x 23 switch envelope and one long service envelope.  CurvedLid remains a separate cable-cover decision; do not turn its low cable notches into a 69 mm window unless explicitly requested.
- **Real alarm horn - INSIDE, on the floor behind the switch.**  The bought part is a 12V 15W siren horn on a swivel foot: owner-measured 102 mm wide x 105 mm long x 102 mm high, with the tightening bolts standing off the back.  (An earlier reading of it as a 104 mm drum standing upright on the cap was wrong in shape and in footprint; every clearance number computed against that shape was void.)  It lies down with its axis front-to-back and its mouth facing the front panel.  It **cannot be tilted in there** - a 20 degree tilt already wants more headroom than the 114 mm cavity has.
- **Why the brain case moved +47 mm:** a full search of the tub against every device *and* every piece of tub hardware found **no 102 x 120 x 102 mm void anywhere in the box** as it was laid out.  The blocker was the brain case, which hangs across the middle from Z36.6 down, leaving only 70 mm of full-height width beside it - 17 mm once you also need 120 mm of clear depth past the extension strip.  `CASE_TO_CAP_X = 47.0` slides the case toward the MikroTik so the switch-side column opens up.  The case then spans X = -20..114, leaving 23 mm to the +X wall for the J4/J5 wires, and its cap bosses land at X = -11.5 / +105.5, Y = -30 / +50.  `FIR_Interface.validate()` refuses to load if that shift is ever dialled back without moving the horn.
- **Horn placement and mount:** body envelope X = -124..-22, Y = -27.5..77.5, Z = 9..111 (mouth face at Y77.5, **4 mm** behind the switch cradle's back wall so its snap hooks keep their ~3 mm flex travel and the switch stays serviceable; 6 mm under the cap roof).  `FIR_Shell.horn_floor_mount()` builds three 14 mm x **6 mm** floor pads on the measured isosceles triangle with 3.4 mm pilots **7 mm** deep: an M4 x 10 bottoms exactly at the pilot floor with 2 mm of tub floor under it - **never longer than M4 x 10**.  Measured clearances: 1.0 mm to the cap side-bolt bosses on the -X wall, 2.0 mm to the brain case.  **Not measured:** `HORN_FOOT_FROM_MOUTH = 99.0` (mouth face -> foot centre) is an assumption; the three pads follow it, and both scripts report it every run.  **Measured bell taper (16 Aug 2026):** D74 at the rear, D76 at the middle, D83 at three-quarters, D102 only at the mouth; bracket stand ~29 mm wide.  `HORN_PROFILE` in the contract carries this and `FIR_ShellCheck` draws the bell as stepped cylinders, so clearances are computed from the real flare, not a 102 mm box: 14 mm to the mid-box wall-boss row, 1-2.4 mm at the mouth row (conservative radial vs true circle), 2 mm to the brain case at the mouth only, 4 mm to the switch cradle, 6 mm under the roof.  The mouth is still what forces the 47 mm brain-case shift - the taper does not reopen that decision.
- **Horn foot fastening - SLED, all three bolts real (16 Aug 2026, replaces the rejected peg):** the horn's own bracket arm and rear tightening bolts hang over its foot holes, so NO in-box driver path to any foot bolt exists.  The owner rejected losing a bolt, so the design uses the standard adapter-plate pattern: a printed **FIR HORN SLED** (76 x 88 x 3 mm plate, three 14 mm bosses on the measured foot triangle, 3.4 mm pilots 5.5 mm deep - **M4 x 8 max**, the pilot stops 0.5 mm above the plate bottom).  All three foot bolts are driven ON THE BENCH where everything is open.  The bolted-up horn+sled is then held by TWO M4 x 10 wing screws alone (the four-sided curb pocket was DELETED 18 Aug 2026 - owner: "we don't need this cage for horns, just the bolt is enough") at (-93, -65) and (-53, -65) - on wing tabs sticking out BEHIND the foot, bell and bracket - clamp it to 14 x 6 mm floor pads with 7 mm pilots.  Both wing corridors verified open to the roof.  Service: undo two screws, lift the whole horn+sled out.  If `HORN_FOOT_FROM_MOUTH` (assumed 99 mm) measures differently, only the small sled reprints.
- **Water scope (owner decision, 16 Aug 2026): top-rain-tight only.**  Requirement is that rain from the top never reaches the components; dripping out of the bottom is fine and expected.  Verified by geometry: the roof is continuous, every roof edge sheds onto the OUTSIDE of the cap skirt, the skirt-to-tub gap opens downward at Z65 below the tub rim at Z80, water on the cap front wall drips onto the cover top and runs off the front, and anything wicking through the 0.5 mm front seam lands inside the cover hood and drains out the downward cable notches.  No gaskets, keyhole plugs or port hood are wanted - do not add them.
- **Assembly order (full box, updated 17 Aug 2026 for the wall mount):** (0) level and screw FIR_WallPlate to the wall (up to six 4.5 mm screws, heads pocketed inside the plate); (1) on the bench: bolt the horn's foot to the printed sled with three M4 x 8; in the tub: drop the horn+sled into the floor curb and drive the two M4 x 10 wing screws behind it; (2) the switch and router can go in before or after - the 4 mm mouth gap keeps the switch's snap hooks serviceable either way; (3) extension strip + adapters + strap; (4) HANG THE TUB: slide it down the plate until the cleat wedges home, then drive the two M4 x 10 floor locks straight down from inside at (+-129, -130) - they are unreachable once the cap is on, BY DESIGN (anti-theft); (5) BottomLid: 6 front M3 into their visible pads - all six verified to land on tub pilots through the mirror mapping; (6) cover slides on, 2 lock screws into their dished seats; (7) the cap (brain stack already assembled inside it per the mandatory case->PCB->tray order) drops straight down - the hidden brain case descends with 2.0 mm beside the horn and lands 1.1 mm over the MikroTik, so lower it slowly and square; (8) 8 cap screws at Z72, four per side wall, all drivable on the wall.
- **No sound vents - owner decision (16 Aug 2026).**  A slotted window was cut through both front parts and then taken back out: Francis's judgement is that this siren is loud enough that a plastic box will not meaningfully muffle it, so the front panel, the cover and the lid all stay closed.  `HORN_VENTS = False` records that in the shared contract and `FIR_ShellCheck` restates it every run.  **Do not re-add a grille anywhere without asking** - it has already been tried and removed once.
- **Cap print-flip gotcha (now load-bearing):** `FIR_Shell` prints the deep cap roof-DOWN and it is physically flipped left/right on assembly, so a feature at cap-local +X lands at assembled -X.  While the brain-boss pattern was symmetric this was invisible; now the case is 47 mm off centre it is not, and `build_top_lid()` mirrors the assembled X exactly once.  Any future asymmetric cap feature must do the same.
- **Confirmed Tenda switch DC jack, and why the switch moved.**  6 mm barrel jack on the switch's own right-hand end face (shell -X), centre 8.3 mm forward of its rear.  At the old X = -85 that face had only 11.0 mm of air to the side wall, so a straight plug could not go in.  Francis asked for the switch to move inboard so the cable can be inserted from the side: **`POE_CX` is now -71.0**, the switch spans X = -112..-30, and the jack face has **25.0 mm** of air - enough for a straight barrel plug (~14 mm) plus the cable turn.  The switch cradle's back side post on that end is still clipped to 8 mm so nothing stands across the jack.  Jack **height** on that face is still unmeasured; 11.5 mm above the switch base (mid-height) is an assumption reported on every run.  If the jack turns out to be on the switch's other end, flip `POE_JACK_SIDE` to `+1.0`.
- **The switch's position is now in the shared contract, not in four files.**  `POE_CX`, `POE_W/D/H`, the 68.8 x 11.5 mm port slot and the front-cover cable-notch pattern all live in `FIR_Interface.py`.  `FIR_Shell` (cradle), `FIR_BottomLid` (service slot, mirrored to -POE_CX), `FIR_CurvedLid` (`poe_cable_notch_x()`) and `FIR_ShellCheck` derive from it.  The switch could not be moved safely while each of those held its own copy of where it was.
- **Large-shell closure - FOUR screws, two per side wall (updated 18 Aug 2026, owner: "two sides each, not four"):** the 281 mm inner top cap slides over the 280 mm tub with 0.5 mm nominal side/back clearance and 15 mm vertical overlap; the 0.5 mm is SLIDE clearance so the cap can go on at all, and the three 20 x 2 x 2 mm crush ribs (0.3 mm PETG interference) plus the screws take it out once closed.  Fasteners: TWO external side M3 per wall at Y = -85/+85, both at Z = 72, screwing horizontally through the skirt into 12 mm wall bosses with `M3_BOSS_PILOT_D` pilots.  The 16-Aug back pair at X = +/-115 was REMOVED on 17 Aug: it could not be driven with the box hanging on its wall (8 mm of air behind it); the Y-118 row just off the back corners replaces it and the back edge keeps its two snap detents.  Every cap screw is drivable with the box wall-hung.  Verified: all 8 cap holes land on their tub pilots, including through the assembly flip (tools/fusion_offline_check.py).  This is a mechanical closure, not a gasketed/IP-rated seal - see the top-rain decision.  **Self-click detents (owner request, 16 Aug 2026):** six stepped bumps on the tub's outer walls (two per side wall at Y = +/-45, two on the back wall at X = +/-45; 8 x 1.2 mm proud, band Z71-76 with a printable half-proud upper step as the ramp and a flat catch underside) snap into matching 10 x 6.5 mm through-windows in the cap skirt (assembled Z70-76.5).  Pushing the cap straight down flexes the skirt ~0.7 mm over the bumps for the last few mm and it CLICKS home; the flat catches then hold the cap by themselves, and the 8 screws remain the permanent lock.  Verified: all 12 bump bands land inside their 6 windows through the assembly flip; detents clear the screw rows, crush ribs and keyholes (validated in `FIR_Interface`).  `FIR_ShellCheck` v37 draws bright marker pins on every screw axis and click pad, because 3.4 mm holes in transparent bodies are invisible on screen and were read as 'no bolts'.
- **The front cover is CURVED and the joint is SEAMLESS (owner, 19 Aug 2026: "like Ubiquiti M2 covers - you won't know there is a joining").**  The quarter-ellipse shoulder (tangent-vertical off the panel, tangent-horizontal at the top) now lands **FLUSH with the cap roof's OUTER surface at Z120** and its tip butts the roof edge with a **single 0.3 mm shadow line** at Y143.3 - the commercial trick: flush outer surfaces, hairline parting line, real clearance hidden behind the surface.  From the front and above the box reads as one shell: flat panel, curve, roof.  The busy Z83 stack (cover top, groove/tongue, cap front wall) is fully hidden.  **ASSEMBLY ORDER CHANGED: the cover slides on LAST, after the cap is closed** - if the cap dropped with the cover fitted, its skirt would scrape the shoulder tip; the cover's hard end stop on the BottomLid bosses is what sets the hairline.  The numbers live in the contract (`COVER_SHOULDER_*`) and validate() enforces the flush plane and the 0.2..0.6 mm gap.  Built as a 48-segment polyline with a true normal-offset inner wall + two end caps; **print standing on an end wall** (280 mm tall, no supports on the show surface).  Side note: the vertical corner lines where the 280-wide cover meets the 286-wide cap remain as intentional panel lines - only the messy top/middle joint is erased.
- **Front closure:** the BottomLid closes the tub front and is held by six counterbored front M3 screws. The CurvedLid slides from the outside toward the tub over the two shelf rails (0.3 mm guide clearance), then its 249 x 1 x 0.7 mm tongue enters the BottomLid groove with print clearance. Two front M3 screws lock the CurvedLid; there is no snap/detent. The cap front wall intentionally has a 0.5 mm clearance seam above the lid/cover top.
- **Port handedness - resolved:** the BottomLid is a flat plate that is turned **over** onto the tub front, so its source +X becomes shell -X.  The CurvedLid is **not** turned over: it prints face-down and only tips up 90 degrees about X to slide onto the shelf, so its source +X stays shell +X.  The two parts therefore do not share a source frame, and CurvedLid's openings had been written in the BottomLid's mirrored one - every notch landed on the wrong device.  `FIR_CurvedLid` now carries them in true shell X (router notches +34.5..+90.5, switch notches -53..-117, power notch -10).  No notch size and no physical position changed; only the sign was corrected.  `FIR_ShellCheck.check_cover_handedness()` re-proves this every run by testing each notch against the real router/switch spans, so the fault cannot return silently.  `FIR_Assembly` is still a presentation-only reflected view and cannot prove cover orientation.
- `FIR_ShellCheck` is the **all-up inspection model only**, never a print source. It clears bodies named `CHECK: ALL-UP` before rebuilding the actual current tub/cap/front closure, actual brain case, actual tray, component-side-down PCB/module/connector envelopes, router, confirmed PoE switch and its DC jack/plug, extension/adapters, the bought horn over the mount the cap now really carries, and low-floor cable paths. It no longer holds its own copy of the tub or the deep cap - those duplicates were deleted, because a second definition of a printed part is exactly how this model drifts away from what actually prints. It also republishes `FIR_Shell`'s own build notes in its report, so a cradle feature the tub had to trim is visible to anyone running the all-up view instead of `FIR_Shell` directly. Its four PCB-screw and other driver-travel cylinders are inspection-only and hidden by default (`SHOW_DRIVER_ACCESS = False`); set it true only to inspect assembly access, never confuse those transparent separate guides with printed structure. It live-reloads current builders on every run rather than showing a cached older Gadget. It derives its case transform from the actual stack: `(X=0, Y=+10, Z=117 + CAP_LIFT - 10.8 - BODY_Z)`, currently Z = `36.6 + CAP_LIFT` mm. That preserves 1.1 mm clearance above the MikroTik and now 7.1 mm above the confirmed 23 mm-high PoE switch. Its visible mains warning is intentional: the old mains proxy intersected the brain case and must not be treated as a finished route.
- **Visible screw seats everywhere (17 Aug 2026 - Francis's live complaint, fixed):** he exported the box and could not find anywhere to screw the lids together; a flush 3.4 mm hole on a 286 mm face is invisible in a shaded render.  Every outside screw now sits in a feature you can see and feel: the 8 cap screws in 12 mm pads standing 2.5 mm proud of the skirt with 6.5 mm counterbores cut back to the ORIGINAL skirt face (heads seat exactly where they did, so no engagement number moved); the 6 BottomLid screws in the same 12 mm pads 1.5 mm proud with the counterbore re-cut from the pad top (head land grows 0.8 -> 1.9 mm, thread grip 9 -> ~8 mm); the CurvedLid's 2 lock screws in 10 mm dished recesses 1.0 mm into the face (that part prints face-down, so a proud pad would lift it off the bed).  Seat dimensions are shared contract values (`M3_SEAT_*`, `CAP_SEAT_PAD_H`, `LID_SEAT_*`, `COVER_SEAT_*`).
- **MOUNTING ORIENTATION - the box is a WALL PANEL (owner, 18 Aug 2026, read this first).**  It is NOT hung sideways off its back wall: the big flat 280 x 280 **FLOOR lies against the wall** and the box stands 120 mm out into the room, so the cap - with the centred 3.5" screen and the two LEDs - faces the viewer.  In model coordinates **+Z points OUT of the wall, not up**, and the wall contact face is the floor underside at Z0.  Working assumption: the port face (+Y) points DOWN the wall so cables hang and drip clear.  **Internal retention: SETTLED (owner, 18 Aug 2026) - do not redesign.**  The cradle snaps, extension-strip lips and adapter strap were designed for exactly this wall orientation and everything is trapped in; they carry their parts in shear by intent.
- **PRINTER LIMIT drives the outside dimensions (owner, 18 Aug 2026): Creality Ender 3 Plus, 300 x 300 x 340 mm.**  The tub is already 280 square, so **no printed part may grow outside the 280 footprint**.  This killed the projecting-tab mount (it made the tub 328 mm wide - unprintable, and nobody would have known until the slicer refused it).  `BED_X/BED_Y/BED_Z` + `BED_MARGIN` live in the contract and `tools/fusion_offline_check.py` now measures every printed body against the envelope on every run.  Current: tub **282.4 x 278.2 x 80** (13.6 mm spare), deep cap **291.0 x 286.0 x 55** (only **5.0 mm spare** - the tight one; centre it on the bed, skip the brim, and if it will not go the cap screw seat pads can flatten to buy 5 mm).  FIR_Shell reports both numbers in its popup every run.
- **Wall mount = TWO BLIND KEYHOLES IN THE FLOOR (owner, 19 Aug 2026 - FINAL).**  Simplest possible, like a router or an extension strip: drive two wall screws 258 mm apart (4.5 mm shank, head under 8.5 mm) leaving ~5 mm proud, hang the box through the round entries at (X +-129, Y-110) and let it DROP 15 mm.  **Nothing passes through into the box** (owner requirement, 19 Aug): the keyhole is a BLIND undercut and the screw head ends up buried inside the floor's own thickness - 2.0 mm outer skin (which is what carries the box, and where the entry hole and 5.5 mm shank slot are cut) + 3.5 mm head channel + **2.0 mm inner skin that is never cut**, so no water, dust or insect can enter through the mount and no fastener enters the sealed volume.  Local pad takes the floor to 7.5 mm there.  No bracket, plate or rail to print - the harness fails if such a part reappears, and it proves the watertightness on the solid itself (material probes above every keyhole).  Dead ends, all in git history: back-wall keyholes, cleat plate, back-wall ears, projecting floor tabs (328 mm wide - would not fit the 300 mm bed), six screws through the floor (needed the box gutted), two-rail hanger.
- **The floor stays FLAT for printing (owner requirement, enforced).**  Every mount feature is a recess or a hole cut from the WALL side - never a bump - so the 280 x 280 floor prints face-down flat on the bed with a clean finish.  `tools/fusion_offline_check.py` fails if anything on the tub ever stands proud of that face.
- **Heat-set inserts: SELECTIVE, per-closure (18 Aug 2026 two-AI review; purchase still pending with Francis):** the single global flag was replaced by `CAP_BOSS_INSERTS` / `COVER_LOCK_INSERTS` / `BOTTOM_LID_INSERTS` (all False = 2.6 mm self-tap).  Agreed split: inserts for the often-opened cap + cover locks, self-tap for the rarely-opened BottomLid.  `M3_INSERT_BORE_D = 4.0` is a PLACEHOLDER - set it from the bought insert's datasheet before flipping any flag.  The cover-lock boss was widened 7 -> 10 mm and its screws moved X +-125 -> +-122 (2.1 mm clear of the cover channel rib) so it can actually take an insert one day.  FIR_Shell's popup raises the pending purchase every run.
- **18 Aug 2026 two-AI closure review - all agreed items BUILT and verified:** (1) cap: 1.2 mm 45-degree lead-in chamfer on the skirt's lower inner edge + engraved mirror-safe front arrow inside the roof; (2) the 249 mm cover tongue replaced by four 25 mm locator tabs at X -90/-30/+30/+90; (3) ONE shared rail clearance `COVER_RAIL_CLR = 0.3` in the contract (killed the CLR=0.4-declared / 0.3-hard-coded conflict a reviewer caught) - rails, channels and coupons all derive from it; (4) cover hard end stop: lock bosses end flush at the cover's inner face (Z65.5) and the two screws clamp against them; (5) anti-rattle: one 0.15 mm nub under each channel web, engaging only the last ~5 mm of travel; (6) ORIENTATION KEY: block on the BottomLid rail (shell +X) + matching relief in the cover's shell +X channel - the right cover clears by 2.5 mm, a reversed cover stands ~7 mm proud and refuses to lock; (7) `FIR_ShellCheck PRESENTATION = True` = opaque shells + solid fastener markers for owner review (transparency is for interference hunting only - it was what made attached features read as floating); (8) NEW `FIR_FitCoupons` script: three coupon pairs (full-perimeter 281/280 cap-fit rings, 70 mm cap-wall section with detent + seated screw, 65 mm rail-lock end slice incl. the flipped-cover refusal test) - print and PASS these before any full 280 mm part; (9) wall plate: keep the cleat (tamper resistance, not vault security); hang-test a printed plate at 9 kg for 24 h before deployment.  Full record: LID_FASTENING_DEBATE.md section 6.
- **Offline verification harness exists (17 Aug 2026):** `python tools/fusion_offline_check.py [deployed_dir]` stubs the adsk API, runs every script's REAL run(), and checks screw alignment through both assembly flips, seat geometry, the cleat mate, driver corridors and the ShellCheck all-up in both modes.  Run it on the workspace AND deployed trees after every geometry change; it exits non-zero on any failure.
- **Front-closure tamper sensing: reed + press-fit magnet (owner, 18 Aug 2026):** the D12.1 (measured) disc magnet press-fits (pocket D12.05, 2 mm PETG rim) into a 16 x 16 pad on the CurvedLid's inner front face at shell X+15, hidden 1.2 mm behind the outer face - nothing shows outside.  The reed lies along X in a 3.2 x 2.0 recessed groove in the BottomLid shelf directly in the magnet's slide path (glue it in); wires run back along the shelf and enter the box through a D4 hole at shell (X+15, Z8), between the router frame wall and the port row, then on to the ESP32 brain.  Closed cover holds the magnet ~10.3 mm head-on from the reed; sliding the cover off opens it within a few mm.  One reed covers the whole front: the BottomLid cannot be removed without removing the cover first (its screws are inside the hood).  Magnet is MEASURED D12.1 x 4.7 (thickness confirmed by owner 18 Aug; the cover pad grew to 6.5 mm proud to hold it).  BENCH-TEST the real reed+magnet pair at ~11 mm before printing.
- **TOP-CAP tamper pair (owner, 18 Aug 2026):** a second D12.1 x 4.7 magnet press-fits into a 16 mm pillar hanging from the cap roof to Z75 at assembled (X-60, Y+125) - over the switch bay, in front of the tub's top rail, clear of the brain case, the cradle hooks and the rail.  The pocket opens DOWNWARD (a clean vertical bore in the roof-down cap print; the pillar X is MIRRORED once like the brain bosses - flip trap).  The reed lies in a 3.2 x 2.0 groove in the rail's FRONT face (recessed so the descending pillar passes 0.5 mm in front of it); seated distance ~10.4 mm, lifting the cap breaks it immediately.  Reed wires join the tub-to-cap service loop beside the horn lead.  BENCH-TEST this pair too - the coupling here is magnet-face-down / reed-beside, weaker than head-on.  Side effect caught during this work: the D12 BottomLid seat pads at +-132 clipped the cover's end walls by 0.5 mm - that pair is now D10, and the harness now rejects any face feature reaching |X|137.
- **INDOOR vs WEATHERPROOF variants - one flag (owner, 18 Aug 2026):** `INDOOR_SCREEN` in FIR_Interface.py.  True (current) = the Landzo 3.5" TFT (UNO/Mega shield, KES2000) shows through a window in the CAP ROOF: the module drops into a 1 mm registration seat cut into the roof's inner face at assembled (X+25, Y+108), glued/clamped from below, wired to the brain PCB's J4; plus two 5 mm holes at (78,100) green / (78,116) red for the Arduino-style indicator LEDs (push through, superglue).  **This roof is NOT top-rain-tight - the owner's explicit trade for the indoor model**; ShellCheck's rain note states it honestly.  False = the sealed continuous roof exactly as before (the weatherproof model will instead hide a screen inside the CurvedLid - future work).  SCREEN DIMS ARE ASSUMED (PCB 86.5 x 57, window 76 x 51, typical UNO-shield 3.5"): `SCREEN_MEASURED = False` - measure the real board before printing the cap.  The seat clears the brain-case plan, its bosses and the magnet pillar (validated); the roof front arrow moved to X-90 to make room.  All these roof features are asymmetric -> mirrored exactly once in build_top_lid (flip trap).
- The small case visual direction is clean and minimal: soft corners, an uninterrupted flat top roof for a clean print finish, functional side vents, and the slim J4/J5 wire slot. Do not add top panels, display windows, rings, rails, logos, engravings, or decorative I/O trim unless the user explicitly asks.
- Set `SHOW_TRAY = True` in `FIR_ModuleGadget.py` only to inspect the transparent fit view; set it back to `False` for print export.
