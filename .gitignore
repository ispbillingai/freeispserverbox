# real credentials never go to GitHub — keep them in secrets.h next to the .ino
secrets.h

# Arduino build output
build/
*.bin.tmp

# Windows noise
Thumbs.db
desktop.ini

# KiCad / PCB generator artifacts
__pycache__/
pcb/drc.json
*.kicad_prl
*-backups/
